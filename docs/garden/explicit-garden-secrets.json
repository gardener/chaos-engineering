{
    "title": "Title goes here...",
    "description": "Description goes here...",
    "method": [
        {
            "type": "action",
            "name": "Name goes here...",
            "provider": {
                "type": "python",
                "module": "chaosgarden.garden.actions",
                "func": ...,
                "secrets": ["garden"],                  # may be set and must then refer to secrets authenticating against Gardener or...; field name free
                                                        # can be omitted and then the env var `$KUBECONFIG` is implicitly used to access Gardener
                "arguments": {
                    ...
                }
            }
        }
    ],
    "configuration": {
        "garden": {
            "project": ...,
            "shoot":   ...
        }
    },
    "secrets": {
        "garden": {
            "kubeconfig_struct": {...}                  # You can hand in the full struct or...
            "kubeconfig_file": "/.../kubeconfig.yaml"   # ...the path to the kubeconfig file or...
            "kubeconfig_envvar": "..."                  # ...the env var that points to such a file
        }
    }
}
